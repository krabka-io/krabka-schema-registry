{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "krabka-schema-registry.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "krabka-schema-registry.labels" . | nindent 4 }}
{{- end }}
